package pl.piasta.camperoo.common.exception;

public enum ErrorCode {
    // common
    ILLEGAL_STATE,
    VALIDATION_ERROR,

    // address
    ROUTE_CALCULATION_ERROR,

    // user
    USER_NOT_FOUND
}
